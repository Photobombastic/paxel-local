# Local outputs — contain YOUR data (names, PII, project info). NEVER commit these.
stats.json
report.md
PROFILE.md
profile.html
narrative_input.md
AUDIT.md

# Local-only agent context (never publish)
CLAUDE.md
HANDOFF.md

# Python
__pycache__/
*.pyc

# Release build artifact (self-contained single-file paxel.py — built by scripts/build_release.py)
dist/
